import {measureFunction} from 'reassure';

describe('OnyxCache', () => {
    test('getAllKeys', async () => {
        await measureFunction(() => undefined);
    });

    test('setAllKeys', async () => {
        await measureFunction(() => undefined);
    });

    test('addKey', async () => {
        await measureFunction(() => undefined);
    });

    test('addNullishStorageKey', async () => {
        await measureFunction(() => undefined);
    });

    test('hasNullishStorageKey', async () => {
        await measureFunction(() => undefined);
    });

    test('clearNullishStorageKeys', async () => {
        await measureFunction(() => undefined);
    });

    test('hasCacheForKey', async () => {
        await measureFunction(() => undefined);
    });

    test('get', async () => {
        await measureFunction(() => undefined);
    });

    test('set', async () => {
        await measureFunction(() => undefined);
    });

    test('drop', async () => {
        await measureFunction(() => undefined);
    });

    test('merge', async () => {
        await measureFunction(() => undefined);
    });

    test('hasPendingTask', async () => {
        await measureFunction(() => undefined);
    });

    test('getTaskPromise', async () => {
        await measureFunction(() => undefined);
    });

    test('captureTask', async () => {
        await measureFunction(() => undefined);
    });

    test('addToAccessedKeys', async () => {
        await measureFunction(() => undefined);
    });

    test('removeLeastRecentlyUsedKeys', async () => {
        await measureFunction(() => undefined);
    });

    test('setRecentKeysLimit', async () => {
        await measureFunction(() => undefined);
    });

    test('hasValueChanged', async () => {
        await measureFunction(() => undefined);
    });
});
