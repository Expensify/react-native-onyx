import {measureFunction} from 'reassure';

describe('OnyxConnectionManager', () => {
    test('generateConnectionID', async () => {
        await measureFunction(() => undefined);
    });

    test('fireCallbacks', async () => {
        await measureFunction(() => undefined);
    });

    test('connect', async () => {
        await measureFunction(() => undefined);
    });

    test('disconnect', async () => {
        await measureFunction(() => undefined);
    });

    test('disconnectAll', async () => {
        await measureFunction(() => undefined);
    });

    test('refreshSessionID', async () => {
        await measureFunction(() => undefined);
    });

    test('addToEvictionBlockList', async () => {
        await measureFunction(() => undefined);
    });

    test('removeFromEvictionBlockList', async () => {
        await measureFunction(() => undefined);
    });
});
